{
  "name": "@voxor/lineage-cli",
  "version": "1.1.0",
  "description": "Command-line runner for Lineage prompt evolution \u2014 for CI, scripts, and AI agents.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "lineage": "./dist/index.js"
  },
  "files": [
    "dist",
    "skill",
    "README.md"
  ],
  "scripts": {
    "build": "tsup",
    "test": "vitest run",
    "type-check": "tsc --noEmit",
    "prepack": "node scripts/copy-skill.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@voxor/lineage-core": "^1.1.0",
    "conf": "^10.2.0",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "@types/uuid": "^9.0.7",
    "tsup": "^8.0.0"
  }
}
